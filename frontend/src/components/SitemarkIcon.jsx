import * as React from "react";
import Box from "@mui/material/Box";

export default function SitemarkIcon() {
  return (
    <Box
      sx={{
        height: 21,
        width: 100,
        mr: 2,
        fontWeight: "bold",
        color: "primary.main",
        userSelect: "none",
      }}>
      Mylody Lite
    </Box>
  );
}
